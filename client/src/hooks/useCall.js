// client/src/hooks/useCall.js
// Re-export from CallContext — the single source of truth for call state.
// All components should use { useCall } from this file or from CallContext directly.
export { useCall } from "../context/CallContext";
